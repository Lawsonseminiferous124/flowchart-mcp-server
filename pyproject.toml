[project]
name = "flowchart-mcp-server"
version = "0.1.0"
description = "A local MCP server that lets Claude generate academic-quality flowcharts from natural language or code."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "graphviz>=0.21",
    "mcp[cli]>=1.27.1",
]
